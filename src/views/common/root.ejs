<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
        }

        .header {
            display: flex;
            justify-content: flex-end;
            padding: 1rem;
        }

        .button-group {
            gap: 1rem;
            display: flex;
        }

        .header-button {
            padding: 0.8rem 1.5rem;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            font-size: 1rem;
        }

        .header-button:hover {
            background-color: #45a049;
        }

        .welcome-container {
            text-align: center;
            margin-top: 15vh;
        }

        .welcome-text {
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 1rem;
        }

        .slogan {
            font-size: 1.5rem;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="button-group">
            <a href="/passport" class="header-button">Register Partpost</a>
            <a href="/login" class="header-button">Login Admin</a>
        </div>
    </div>

    <div class="welcome-container">
        <h1 class="welcome-text">Hello!</h1>
        <p class="slogan">Welcome to Our Website</p>
    </div>
</body>
</html>